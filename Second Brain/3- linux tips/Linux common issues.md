#### If Ubuntu fails to launch at boot:
* Could be fuse was installed
* boot into Ubuntu recovery mode by selecting at Grub menu
* enable networking
* drop to root terminal
* `apt install ubuntu-desktop`
* `reboot`





